# Vite exposes only variables prefixed with VITE_
# Copy to client/.env.local for local development (DO NOT COMMIT)

# API Configuration
VITE_API_URL=http://localhost:3001

# GitHub Pages (optional): set to "/<repo-name>/" during build
VITE_BASE=/
