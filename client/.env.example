# Vite exposes only variables prefixed with VITE_
# Copy to `client/.env.local` for local/dev (DO NOT COMMIT)
VITE_API_BASE_URL=http://localhost:5000

# GitHub Pages (optional): set to "/<repo-name>/" during build
VITE_BASE=/
